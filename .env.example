# OpenWeatherMap API key - get one at https://home.openweathermap.org/api_keys
# PRIVATE server-side variable. The browser never sees it; all OWM calls go through /api routes.
# Migrated from NEXT_PUBLIC_WEATHER_KEY (which shipped the key to every visitor's bundle).
WEATHER_API_KEY=your_openweathermap_api_key
