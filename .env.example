# OpenWeatherMap API key — get one at https://home.openweathermap.org/api_keys
# NOTE: NEXT_PUBLIC_ vars are embedded into the browser bundle. Never commit real keys.
NEXT_PUBLIC_WEATHER_KEY=your_openweathermap_api_key
